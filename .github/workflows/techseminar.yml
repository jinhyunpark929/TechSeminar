name: TechSeminar
on:
  push:
env:
  IMAGE_NAME: jamie929/spotlink:jenkins-1.0
jobs:
  Git_Checkout:
    name: Git Checkout
    runs-on: ubuntu-latest
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - name: echo message
      run: "echo ð\x9F\x93¥ GitHubì\x97\x90ì\x84\x9C ì\x86\x8Cì\x8A¤ì½\x94ë\x93\x9C clone ì¤\x91..."
    - name: sh
      shell: bash
      run: rm -rf spotlink && git clone -b main https://github.com/jinhyunpark929/The-era-of-github-actions.git spotlink
  Gradle_Build:
    name: Gradle Build
    runs-on: ubuntu-latest
    needs: Git_Checkout
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - name: echo message
      run: "echo â\x9C\N Gradle ë¹\x8Cë\x93\x9C ì\x8B\x9Cì\x9E\x91 (í\N\x8Cì\x8A¤í\x8A¸ ì\x83\x9Dë\x9Eµ)"
    - name: sh
      shell: bash
      run: "./gradlew clean build -x test"
      working-directory: spotlink
  Docker_Build:
    name: Docker Build
    runs-on: ubuntu-latest
    needs: Gradle_Build
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - name: echo message
      run: "echo ð\x9F\x90³ Docker ì\x9D´ë¯¸ì§\x80 ë¹\x8Cë\x93\x9C ì¤\x91..."
    - name: sh
      shell: bash
      run: docker build -t $IMAGE_NAME .
      working-directory: spotlink
  Docker_Login_Push:
    name: Docker Login & Push
    runs-on: ubuntu-latest
    needs: Docker_Build
    steps:
    - name: checkout
      uses: actions/checkout@v4.1.0
    - name: sh
      shell: bash
      run: "echo \"ð\x9F\x94\x90 DockerHub ë¡\x9Cê·¸ì\x9D¸ ì¤\x91...\"\necho \"$DOCKERHUB_PASSWORD\" | docker login -u \"$DOCKERHUB_USERNAME\" --password-stdin\ndocker push $IMAGE_NAME"
      env:
        DOCKERHUB_USERNAME: "${{ secrets.DOCKERHUB_CREDS_DOCKERHUB_USERNAME }}"
        DOCKERHUB_PASSWORD: "${{ secrets.DOCKERHUB_CREDS_DOCKERHUB_PASSWORD }}"
